#ifndef SERVER_H
#define SERVER_H

#include <string>

/**
 * Runs the iPerfer server.
 * 
 * @param port The port number on which the server will listen for incoming connections.
 */
void runServer(int port);

#endif // SERVER_H